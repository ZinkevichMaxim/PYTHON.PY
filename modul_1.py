# def max_1(a, b):
#     if a > b:
#         return(a)
#     else:
#         return(b)
#     return прерывает функцию следовательно else полюбому не сработает. Поэтому можно убрать

def max_1(a, b):
    if a > b:
        return(a)
    return(b)